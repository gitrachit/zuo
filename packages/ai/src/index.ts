// Copilot AI layer (docs/copilot-architecture.md).
// PR A: deterministic tool layer + model router (no LLM, no API key).
// The Sonnet tool-use loop, numeric validator, and guardrails land in PR B.

export * from "./models";
export * from "./tools";
