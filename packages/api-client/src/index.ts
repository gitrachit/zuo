// Typed API client shared by web/mobile — grows with the API surface (phase 1+).
// Phase 0 stub exists so the workspace, test runner, and CI exercise this package.

export const PACKAGE_NAME = "@zuo/api-client";
